export class AddressBookModel {

     firstName!: string;
     lastName!: string;
     email!: string;
     phoneNumber!: number;;
     city!: string;
     state!: string;
    zip!: number;
    constructor(firstName: string, lastName: string, email: string, phoneNumber: number, city: string, state: string, zip: string) {
    
    }
}
